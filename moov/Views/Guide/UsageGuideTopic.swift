//
//  UsageGuideTopic.swift
//  moov
//

import Foundation

/// 이용 가이드 한 항목. 기능(엔터티) 단위로 분리된 텍스트 사용법 안내. UC-12, FR-23.
struct UsageGuideTopic: Identifiable {
    let id: String
    let title: String
    /// MVP 단계에서는 마크다운을 렌더링하지 않고 그대로 텍스트로 노출한다.
    let body: String
}

/// 작성이 완료된 이용 가이드 항목만 여기 추가한다 — 미작성 항목은 목록에 노출하지 않는다.
enum UsageGuideContent {
    static let topics: [UsageGuideTopic] = [
        UsageGuideTopic(
            id: "exercise-catalog",
            title: "종목 카탈로그 관리",
            body: """
            "종목"은 운동 이름(예: Back Squat, Deadlift)을 등록해두는 목록이에요. \
            세션이나 템플릿에서 블록을 만들 때 이 목록에서 골라 쓰기 때문에, 미리 등록해두면 다음에 더 빠르게 기록할 수 있어요.

            1. 관리 탭으로 이동한 뒤 "종목 카탈로그"를 선택하세요.
            2. 화면 오른쪽 위의 "종목 추가" 버튼(+ 아이콘)을 누르세요.
            3. "이름" 항목에 종목명을 입력하세요. 이 값만 채우면 저장할 수 있어요.
            4. 필요하면 "카테고리 (선택)"에 웨이트리프팅, 체조, 컨디셔닝처럼 분류를 적어두세요. 비워둬도 됩니다.
            5. 무게를 항상 같은 단위(lb 또는 kg)로 기록하고 싶다면 "기본 무게 단위 (선택)"에서 "기본 단위 지정"을 켜고 원하는 단위를 고르세요.
            6. 오른쪽 위 "저장"을 누르면 목록에 추가됩니다. 이름을 채우지 않으면 "저장" 버튼이 눌리지 않아요.

            수정하고 싶을 때는 목록에서 종목을 한 번 탭하면 같은 입력 화면이 뜨고, 값을 바꾼 뒤 "저장"을 누르면 됩니다.

            삭제하고 싶을 때는 목록에서 종목을 왼쪽으로 밀어서 "삭제"를 누르세요. 이미 세션이나 PR에서 그 종목을 쓴 적이 있다면 "이미 N개 기록에 사용 중입니다. 삭제해도 과거 기록의 종목명은 유지됩니다"라는 안내가 뜨는데, 삭제해도 예전에 기록한 세션/PR에 남아있는 종목 이름은 그대로 보여요 — 과거 기록이 사라지거나 이름이 비는 건 아니니 걱정하지 않아도 됩니다.
            """
        ),
        UsageGuideTopic(
            id: "tag-catalog",
            title: "태그 카탈로그 관리",
            body: """
            "태그"는 웜업/본운동/보조운동처럼 블록을 구분하는 이름표예요. 세션이나 템플릿에서 블록을 만들 때 이 목록에서 골라 붙이기 때문에, 미리 등록해두면 편해요.

            1. 관리 탭으로 이동한 뒤 "태그 카탈로그"를 선택하세요.
            2. 화면 오른쪽 위의 "태그 추가" 버튼(+ 아이콘)을 누르세요.
            3. "이름" 항목에 태그명을 입력하세요. 이 값만 채우면 저장할 수 있어요.
            4. 필요하면 "색상 (선택)"에서 원 모양 색상 중 하나를 눌러 골라두세요. 같은 색을 다시 누르면 선택이 풀립니다. 비워둬도 됩니다.
            5. 오른쪽 위 "저장"을 누르면 목록에 추가됩니다. 이름을 채우지 않으면 "저장" 버튼이 눌리지 않아요.

            수정하고 싶을 때는 목록에서 태그를 한 번 탭하면 같은 입력 화면이 뜨고, 값을 바꾼 뒤 "저장"을 누르면 됩니다.

            삭제하고 싶을 때는 목록에서 태그를 왼쪽으로 밀어서 나오는 삭제 버튼을 누르세요. 종목과 달리 별도 확인 창 없이 바로 지워집니다. 이미 어떤 블록에 붙여둔 태그를 지워도 그 블록이나 세션 기록 자체가 사라지지는 않고, 그 블록에서 태그만 떨어져 나갈 뿐이에요.
            """
        ),
    ]
}
